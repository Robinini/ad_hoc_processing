from distutils.core import setup
setup(
  name = 'ad_hoc_processing',
  packages = ['ad_hoc_processing'], # this must be the same as the name above
  version = '0.1',
  description = 'A library to create a named group on a network to carry out distributed processing functions',
  author = 'Robin Dainton',
  author_email = 'XXXXXXXXXX',
  url = 'https://github.com/Robinini/ad_hoc_processing', # use the URL to the github repo
  download_url = 'https://github.com/Robinini/ad_hoc_processing/archive/master.zip', # I'll explain this in a second
  keywords = ['ad-hoc', 'processing', 'pyre', 'zeromq'], # arbitrary keywords
  classifiers = [],
)
